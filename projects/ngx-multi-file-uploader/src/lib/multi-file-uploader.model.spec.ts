import { HttpClient } from '@angular/common/http';
import { MultiFileUploader } from './multi-file-uploader.model';

describe('MultiFileUploader', () => {
  it('should create an instance', () => {
    expect(new MultiFileUploader()).toBeTruthy();
  });
});
